# Store the real file only at /opt/dentnode/apps/ai.dentnode.com/env/secrets.env.
# It must be root-owned mode 0600 and must never be committed.
INTERNAL_API_KEY=
OPENROUTER_API_KEY=
D10_INTERNAL_KEY=
