# google-adk provides the agent framework; litellm routes every model through
# OpenRouter via google.adk.models.lite_llm.LiteLlm ("openrouter/..." models).
google-adk>=2.0.0,<3.0.0
litellm>=1.50.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.7.0
python-dotenv>=1.0.0

# Scan Review (standalone mesh QA module). trimesh's core loaders + topology
# need only numpy — the optional scipy/rtree/embree extras are deliberately NOT
# installed, so the image stays small and the analysis stays pure-Python.
trimesh>=4.4.0
numpy>=1.26.0
